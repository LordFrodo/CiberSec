find if the websites has the .git exposed and run 

git-dumper https://0ab70017033ec24083a2d79a003b0057.web-security-academy.net/ repo

where the URL is the URL of the site with the .git exposed and "repo" is the name of the file when be located the clone of this files.